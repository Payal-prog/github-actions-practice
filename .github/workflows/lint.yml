# What is the goal of your workflow

# Goal: Whenever someone changes the python code, the linter will check if the syntax, format is correct or not

name: Auto Linter

on:
  push:
    paths: "*.py"

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout/clone the code
        uses: actions/checkout@v4
      
